export const CAPACITY_DATA = Symbol('CAPACITY_DATA');

/** Reuses the shared `CapacityDataPort` contract owned by @wispace/student-report. */
export type { CapacityDataPort } from '@wispace/student-report/core';
