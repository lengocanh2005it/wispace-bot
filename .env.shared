# Shared variables for all bots — stored in Vault at secret/data/shared/prd.
# Loaded at runtime by each bot's vault-secrets.ts.
#
# To update a value: Vault UI/API/CLI → secret/data/shared/prd
# No need to edit this file — it is kept for reference only.
