# Slim pre-push gate — the full quality gate (format + lint + typecheck + test + build)
# runs in CI (.github/workflows/pull-request.yml) on every PR and push to main.
# --concurrency=4: avoids Windows OOM crashes at full parallelism.
npx turbo run typecheck test --concurrency=4
