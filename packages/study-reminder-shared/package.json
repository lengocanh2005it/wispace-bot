{
  "name": "@wispace/study-reminder-shared",
  "version": "0.0.1",
  "description": "Platform-agnostic study reminder core for WISPACE bots",
  "private": true,
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.build.json --noEmit",
    "lint": "oxlint",
    "format:check": "oxfmt --check",
    "format": "oxfmt",
    "test": "jest --forceExit --maxWorkers=50%"
  },
  "dependencies": {
    "@nestjs/common": "^11.0.1",
    "@nestjs/config": "^4.0.4",
    "@nestjs/schedule": "^6.0.0",
    "@nestjs/typeorm": "^11.0.1",
    "@wispace/bot-common": "*",
    "@wispace/contracts": "*",
    "@wispace/date-utils": "*",
    "@wispace/reschedule-confirm": "*",
    "@wispace/wispace-client": "*",
    "cron": "^4.4.0",
    "date-fns": "^4.4.0",
    "prom-client": "^15.1.3",
    "typeorm": "^1.1.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^30.5.1",
    "reflect-metadata": "^0.2.2",
    "ts-jest": "^29.4.12"
  },
  "jest": {
    "moduleFileExtensions": [
      "js",
      "json",
      "ts"
    ],
    "rootDir": "src",
    "testRegex": ".*\\.spec\\.ts$",
    "transform": {
      "^.+\\.(t|j)s$": "ts-jest"
    },
    "testEnvironment": "node"
  }
}
